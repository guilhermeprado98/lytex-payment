export const environment = {
  production: true,
  /** Em produção/SSR sem proxy, use a URL pública do Nest (ex.: http://localhost:3000 ou https://api.seudominio.com). */
  apiUrl: 'http://localhost:3000',
};
